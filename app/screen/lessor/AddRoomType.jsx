import RoomForm from "../../component/form/RoomForm";

const AddRoomType = ({ route, navigation }) => {
    return (
        <RoomForm navigation={navigation} route={route} />
    )
}
export default AddRoomType;